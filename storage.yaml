apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: gateway-ns
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: my-storage-class
