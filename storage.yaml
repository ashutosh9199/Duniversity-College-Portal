apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: college-nfs-stateful
provisioner: cluster.local/nfs-subdir-external-provisioner
parameters:
  archiveOnDelete: "false"
reclaimPolicy: Delete
volumeBindingMode: Immediate
